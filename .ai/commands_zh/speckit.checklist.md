---
description: 基于用户需求为当前特性生成自定义 checklist。
---

## Checklist 目的：“Unit Tests for English”

**关键概念**：Checklist 是 **需求写作的单元测试**——用于验证需求的质量、清晰度与完整性，而非验证实现。

**不用于验证/测试实现**：

- ❌ 不是 “按钮能否正确点击”
- ❌ 不是 “错误处理是否工作”
- ❌ 不是 “API 是否返回 200”
- ❌ 不检查代码/实现是否匹配 spec

**用于需求质量验证**：

- ✅ “是否为所有卡片类型定义了视觉层级要求？”（完整性）
- ✅ “‘突出显示’ 是否以具体尺寸/位置量化？”（清晰度）
- ✅ “悬停态要求在所有交互元素上是否一致？”（一致性）
- ✅ “是否为键盘导航定义了无障碍要求？”（覆盖）
- ✅ “当 Logo 图片加载失败时，需求是否定义了行为？”（边界场景）

**比喻**：若你的 spec 是英语代码，checklist 就是它的单元测试套件。你在测试需求是否写得好，而不是实现是否工作。

## 用户输入

```text
$ARGUMENTS
```

在继续之前，**必须**先审视用户输入（若不为空）。

## 执行步骤

1. **准备**：在仓库根运行 `.specify/scripts/powershell/check-prerequisites.ps1 -Json`，解析 FEATURE_DIR 与 AVAILABLE_DOCS 列表。
   - 所有路径必须是绝对路径。
   - 若参数含单引号如 "I'm Groot"，需转义：如 'I'\''m Groot'（或可行时用双引号）。

2. **澄清意图（动态）**：生成最多三个初始澄清问题（无预置问题库），必须：
   - 基于用户表述 + 从 spec/plan/tasks 提取的信号
   - 仅询问会实质改变 checklist 内容的信息
   - 若 `$ARGUMENTS` 已清晰，则跳过对应问题
   - 精准优先于宽泛

   生成算法：
   1. 提取信号：领域关键词（auth、latency、UX、API）、风险指示（“critical”、“must”、“compliance”）、干系人提示（“QA”、“review”、“security team”）、明确交付物（“a11y”、“rollback”、“contracts”）。
   2. 聚类为候选关注点（≤4），按相关性排序。
   3. 推断受众与时机（作者/审阅/QA/release），若未显式说明。
   4. 识别缺失维度：范围广度、深度/严谨度、风险强调、排除边界、可度量验收。
   5. 从以下原型生成问题：
      - 范围收敛：如 “是否包含与 X/Y 的集成触点，还是仅限本地模块正确性？”
      - 风险优先：如 “这些潜在风险领域中，哪些需要强制 gate？”
      - 深度刻度：如 “这是轻量 pre-commit 自检，还是正式发布 gate？”
      - 受众定位：如 “仅作者自检，还是 PR 审阅同行使用？”
      - 边界排除：如 “本轮是否明确排除性能调优项？”
      - 场景缺口：如 “未见恢复流，是否需要包含回滚/部分失败路径？”

   问题格式规则：
   - 若给出选项，生成紧凑表格：Option | Candidate | Why It Matters
   - 选项最多 A–E；若自由回答更清晰则不设表格
   - 不要求用户重复已说内容
   - 不臆测类别，若不确定直接问 “X 是否在范围内？”

   无法互动时的默认值：
   - 深度：Standard
   - 受众：代码相关则 Reviewer (PR)，否则 Author
   - 关注：排名前 2 的相关集群

   输出问题（标注 Q1/Q2/Q3）。若在回答后仍有 ≥2 个场景类别（备用/异常/恢复/非功能域）不清晰，可再问最多两条精准追问（Q4/Q5），并给一句理由（如 “未解决的恢复路径风险”）。总问题 ≤5。若用户拒绝继续，不再升级。

3. **理解用户请求**：结合 `$ARGUMENTS` 与澄清回答：
   - 提炼 checklist 主题（如 security、review、deploy、ux）
   - 汇总用户显式必需项
   - 将关注点映射到分类骨架
   - 从 spec/plan/tasks 推断缺失上下文（不臆测）

4. **加载特性上下文**：从 FEATURE_DIR 读取：
   - spec.md：需求与范围
   - plan.md（若有）：技术细节、依赖
   - tasks.md（若有）：实施任务

   **加载策略**：
   - 仅加载与当前关注点相关的必要部分（避免全量）
   - 偏好将长段落提炼为简洁要点
   - 逐步补充：发现缺口再追加读取
   - 若源文档较长，先生成摘要，再补充必要原文

5. **生成 checklist** —— “需求的单元测试”：
   - 若不存在则创建 `FEATURE_DIR/checklists/`
   - 生成简短、描述性的文件名：
     - 基于领域命名，如 `ux.md`、`api.md`、`security.md`
     - 格式 `[domain].md`
     - 若文件已存在，则附加写入
   - 项目从 CHK001 开始全局递增
   - 每次运行 `/speckit.checklist` 都创建一个新文件（不覆盖已有）

   **核心原则——测试需求而非实现**：
   每条 checklist 关注需求的：
   - **完整性**：必要需求是否齐全？
   - **清晰度**：需求是否明确无歧义？
   - **一致性**：需求之间是否对齐？
   - **可测性**：能否客观验证？
   - **覆盖度**：场景/边界是否覆盖？

   **分类结构** —— 按需求质量维度分组：
   - Requirement Completeness（需求完整性）
   - Requirement Clarity（需求清晰度）
   - Requirement Consistency（需求一致性）
   - Acceptance Criteria Quality（验收标准质量）
   - Scenario Coverage（场景覆盖）
   - Edge Case Coverage（边界场景）
   - Non-Functional Requirements（非功能）
   - Dependencies & Assumptions（依赖与假设）
   - Ambiguities & Conflicts（歧义与冲突）

   **如何编写 checklist 项——“English 单元测试”**：

   ❌ 错误（测试实现）：
   - “验证落地页显示 3 个卡片”
   - “测试桌面悬停态工作正常”
   - “确认点击 Logo 导航首页”

   ✅ 正确（测试需求质量）：
   - “是否明确定义精选卡片的数量与布局？[Completeness]”
   - “‘突出’ 是否以可度量的视觉属性量化？[Clarity]”
   - “悬停态要求在全部交互元素上是否一致？[Consistency]”
   - “是否为键盘导航定义无障碍要求？[Coverage]”
   - “Logo 图片加载失败的回退行为是否已写明？[Edge Case]”
   - “异步数据加载的 loading 状态是否有需求？[Completeness]”

   **条目结构**：
   - 以问题形式，关注“写没写/写得清不清”
   - 聚焦 spec/plan 中的内容
   - 附质量维度标记 [Completeness/Clarity/...]
   - 引用规范段落 `[Spec §X.Y]`，缺失则标 `[Gap]`

   **按维度示例**：

   完整性：
   - “是否为所有 API 失败模式定义错误处理需求？[Gap]”
   - “是否为所有交互元素定义无障碍需求？[Completeness]”
   - “是否定义响应式的移动断点需求？[Gap]”

   清晰度：
   - “‘fast loading’ 是否给出具体时间阈值？[Clarity, Spec §NFR-2]”
   - “‘related episodes’ 的选择标准是否明确？[Clarity, Spec §FR-5]”
   - “‘prominent’ 是否以可测视觉属性定义？[Ambiguity, Spec §FR-4]”

   一致性：
   - “导航需求在各页面是否一致？[Consistency]”
   - “卡片组件需求在落地页与详情页是否一致？[Consistency]”

   覆盖：
   - “是否定义空状态（无数据）需求？[Coverage, Edge Case]”
   - “是否涵盖并发交互场景？[Coverage, Gap]”
   - “是否定义部分加载失败的处理？[Coverage, Exception Flow]”

   可测性：
   - “视觉层级需求是否可客观验证？[Acceptance Criteria, Spec §FR-1]”
   - “‘balanced visual weight’ 是否可测？[Measurability, Spec §FR-2]”

   **场景分类与覆盖**（关注需求质量）：
   - 检查是否存在：主流程、备用、异常/错误、恢复、非功能场景的需求
   - 若缺失：写 “是否明确排除或遗漏该场景？[Gap]”
   - 若涉及状态变更，包含韧性/回滚：“迁移失败时回滚需求是否定义？[Gap]”

   **可追溯性要求**：
   - 至少 80% 条目应含追溯引用
   - 引用格式：`[Spec §X.Y]`，或使用 `[Gap]`、`[Ambiguity]`、`[Conflict]`、`[Assumption]`
   - 若缺少 ID 体系：添加 “是否建立需求与验收标准 ID 方案？[Traceability]”

   **暴露并解决问题**（需求质量问题）：
   - 歧义：如 “‘fast’ 是否量化？[Ambiguity, Spec §NFR-1]”
   - 冲突：如 “§FR-10 与 §FR-10a 导航要求是否冲突？[Conflict]”
   - 假设：如 “对 ‘始终可用的 podcast API’ 的假设是否验证？[Assumption]”
   - 依赖：如 “外部 podcast API 的需求是否记录？[Dependency, Gap]”
   - 缺失定义：如 “‘visual hierarchy’ 是否有可测定义？[Gap]”

   **内容收敛**：
   - 若候选 >40，按风险/影响优先
   - 合并近似重复
   - 若低影响边界案例 >5，可合并为一条：“边界案例 X/Y/Z 是否覆盖？[Coverage]”

   **🚫 严禁**（这些是实现测试而非需求测试）：
   - 以 “Verify/Test/Confirm/Check” + 实现行为的表述
   - 引用代码执行、用户操作、系统行为
   - “显示正确”、“工作正常”、“按预期运行”
   - “点击/导航/渲染/加载/执行” 等实现动作
   - 测试用例、测试计划、QA 流程
   - 实现细节（框架、API、算法）

   **✅ 必须的模式**（测试需求质量）：
   - “是否为 [场景] 定义/记录 [需求类型]？”
   - “‘模糊术语’ 是否以具体标准量化/澄清？”
   - “[Section A] 与 [Section B] 的需求是否一致？”
   - “[需求] 能否被客观度量/验证？”
   - “是否覆盖 [边界/场景] 需求？”
   - “需求是否定义了 [缺失方面]？”

6. **结构参考**：按照 `.specify/templates/checklist-template.md` 的标题、元信息、分类标题、ID 格式生成；若模板缺失，使用：H1 标题、purpose/created 元数据行、`##` 分类小节，内部为 `- [ ] CHK### <条目>`，ID 全局递增从 CHK001 开始。

7. **报告**：输出创建的 checklist 全路径、条目数，并提醒每次运行会生成新文件。摘要包含：
   - 选择的关注领域
   - 深度水平
   - 受众/时间
   - 已纳入的用户显式必需项

**重要**：每次 `/speckit.checklist` 调用都会创建一个以简短描述命名的 checklist，除非文件已存在。这样可以：

- 针对不同类型生成多个清单（如 `ux.md`、`test.md`、`security.md`）
- 用简洁可记的文件名指明用途
- 方便在 `checklists/` 中识别与导航

为避免杂乱，请使用描述性类型并清理不再需要的 checklist。

## 示例 checklist 类型与样例条目

**UX Requirements Quality：** `ux.md`

示例：
- “是否以可测标准定义视觉层级？[Clarity, Spec §FR-1]”
- “UI 元素数量与位置是否明确？[Completeness, Spec §FR-1]”
- “交互状态（hover/focus/active）要求是否一致？[Consistency]”
- “所有交互元素的无障碍要求是否定义？[Coverage, Gap]”
- “图片加载失败的回退是否定义？[Edge Case, Gap]”
- “‘prominent display’ 是否可客观测量？[Measurability, Spec §FR-4]”

**API Requirements Quality：** `api.md`

示例：
- “是否为所有失败场景定义错误响应格式？[Completeness]”
- “限流要求是否量化阈值？[Clarity]”
- “认证要求在各端点是否一致？[Consistency]”
- “外部依赖的重试/超时要求是否定义？[Coverage, Gap]”
- “版本策略是否记录？[Gap]”

**Performance Requirements Quality：** `performance.md`

示例：
- “性能要求是否量化？[Clarity]”
- “关键用户旅程是否有性能目标？[Coverage]”
- “不同负载下的性能要求是否定义？[Completeness]”
- “性能要求是否可客观测量？[Measurability]”
- “高负载退化策略是否定义？[Edge Case, Gap]”

**Security Requirements Quality：** `security.md`

示例：
- “所有受保护资源的认证要求是否定义？[Coverage]”
- “敏感信息的数据保护要求是否定义？[Completeness]”
- “威胁模型是否记录，需求是否与之对齐？[Traceability]”
- “安全需求是否与合规义务一致？[Consistency]”
- “安全失败/泄露响应需求是否定义？[Gap, Exception Flow]”

## 反例（不要这样写）

**❌ 错误——这是实现测试，不是需求测试：**

```markdown
- [ ] CHK001 - Verify landing page displays 3 episode cards [Spec §FR-001]
- [ ] CHK002 - Test hover states work correctly on desktop [Spec §FR-003]
- [ ] CHK003 - Confirm logo click navigates to home page [Spec §FR-010]
- [ ] CHK004 - Check that related episodes section shows 3-5 items [Spec §FR-005]
```

**✅ 正确——这是需求质量测试：**

```markdown
- [ ] CHK001 - Are the number and layout of featured episodes explicitly specified? [Completeness, Spec §FR-001]
- [ ] CHK002 - Are hover state requirements consistently defined for all interactive elements? [Consistency, Spec §FR-003]
- [ ] CHK003 - Are navigation requirements clear for all clickable brand elements? [Clarity, Spec §FR-010]
- [ ] CHK004 - Is the selection criteria for related episodes documented? [Gap, Spec §FR-005]
- [ ] CHK005 - Are loading state requirements defined for asynchronous episode data? [Gap]
- [ ] CHK006 - Can "visual hierarchy" requirements be objectively measured? [Measurability, Spec §FR-001]
```

**关键差异**：

- 错误：测试系统是否工作
- 正确：测试需求是否写好
- 错误：验证行为
- 正确：验证需求质量
- 错误：“是否做了 X？”
- 正确：“X 是否被清晰规定？”

